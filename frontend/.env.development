# Where `uvicorn app.main:app` is listening (Vite proxies /meta, /predict, /analytics, /health here).
# Use 8000 if you start the API on port 8000 instead.
VITE_API_PROXY_TARGET=http://127.0.0.1:8005
VITE_CROP_AI_BASE_URL=http://127.0.0.1:8005
